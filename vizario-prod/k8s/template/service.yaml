apiVersion: v1
kind: Service
metadata:
  name: vizario-prod-svc
  namespace: vizario-prod
spec:
  selector:
    app: fastapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer

