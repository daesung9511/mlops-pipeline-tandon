services:
  backend:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn llava_video_app:app --host 0.0.0.0 --port 8000
    gpus: all
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
